package com.easytier.client

import io.flutter.embedding.android.FlutterActivity

/**
 * The single Flutter entry activity. Kept minimal: the Flutter engine handles
 * rendering and the native VPN service is a separate component.
 */
class MainActivity : FlutterActivity()
